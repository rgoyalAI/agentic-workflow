Fix bug: $ARGUMENTS

1. Read `contexts/PROJECT_CONTEXT.md` and `AGENTS.md` for context
2. Check `./memory/features.md` to understand the affected capability
3. Check `./memory/system-design.md` for component boundaries
4. Check `./memory/decisions.md` for prior decisions that may constrain the fix
5. Reproduce the bug — identify root cause before writing any fix
6. Write a failing test that proves the bug exists
7. Implement the minimal fix
8. Verify: test passes, no regressions, scope is minimal
9. Update `./memory/open-items.md` if this resolves a tracked blocker
