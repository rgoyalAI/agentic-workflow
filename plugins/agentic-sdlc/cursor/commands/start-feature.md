Start implementing feature: $ARGUMENTS

1. Read `contexts/PROJECT_CONTEXT.md` for project orientation
2. Load `AGENTS.md` deterministic context protocol
3. Check `./memory/features.md` for existing capabilities (avoid duplication)
4. Check `./memory/system-design.md` for architecture constraints
5. Check `./memory/progress.md` and `./memory/open-items.md` for related work
6. If a spec exists in `./specifications/` for this feature, load it first
7. Invoke **DecomposeRequirements** with the feature description
8. After stories approved, begin per-story pipeline via **OrchestrateSDLC**

Work from the repository root so relative paths resolve consistently.
