# Coverage Report — Story `{{story_id}}`

## Summary

| Metric | Value |
|--------|-------|
| **Story ID** | `{{story_id}}` |
| **Pipeline / local run** | `{{run_context}}` |
| **Timestamp (UTC)** | `{{timestamp_utc}}` |
| **Overall coverage** | **{{total_pct}}%** |
| **Threshold** | **{{threshold_pct}}%** |
| **Verdict** | `PASS` | `FAIL` |

---

## Threshold rule

Coverage is measured on **{{scope_description}}** (e.g. `src/`, excluding `**/*.d.ts`, `**/generated/**`).

**Policy:** Verdict is **PASS** if `total_pct >= threshold_pct` and **no** required package falls below **{{package_floor_pct}}%** (if applicable).

---

## Per-file / per-package breakdown

| Path | Lines % | Branches % | Notes |
|------|---------|------------|-------|
| {{path_1}} | | | |
| {{path_2}} | | | |

---

## Gaps (when FAIL or advisory)

| Gap | Severity | Suggested test |
|-----|----------|------------------|
| {{gap_1}} | high / medium / low | |
| {{gap_2}} | | |

---

## Artifacts

- **LCOV / Cobertura path:** `{{artifact_path}}`
- **HTML report:** `{{html_report_path}}`

---

## Sign-off

- **Generated by:** {{tool_or_agent}}
- **Next step:** {{merge_or_add_tests}}
