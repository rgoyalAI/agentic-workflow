# Copilot adapter (copy into `.github/`)

This folder is a Copilot adapter for the `adm` plugin.

Copilot doesn't load nested "plugins" automatically, so copy the adapter files into the repo-level directories, typically:
- `.github/agents/` (for `*.agent.md`)
- `.github/workflows/` (for `*.md` agentic workflows)

